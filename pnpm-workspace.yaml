packages:
  - "packages/*"
  - "examples/*"
  - "apps/*"

# pnpm 11: allow esbuild postinstall (needed by Vitest/Vite).
allowBuilds:
  esbuild: true

# Keep 24h age gate; allow known fresh transitive currently in the lockfile.
minimumReleaseAgeExclude:
  - postcss
