# ZwenTS docs site (VitePress)

```bash
pnpm docs:dev      # http://localhost:5173
pnpm docs:build
pnpm docs:preview
```

Content lives in `apps/docs/`. Source of truth for deep specs remains `docs/` in the monorepo (RFCs, reviews).
