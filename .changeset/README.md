# Changesets

For future releases after `0.1.0`:

```bash
pnpm changeset          # record a change
pnpm changeset version  # bump + CHANGELOG
pnpm changeset publish  # npm publish (needs auth)
```

Initial `0.1.0` was set manually — see [docs/publish.md](../docs/publish.md).
